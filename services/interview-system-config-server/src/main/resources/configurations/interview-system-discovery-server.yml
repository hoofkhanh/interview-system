eureka:
  instance:
    hostname: ${HOST_NAME:localhost}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://${OTHER_ADDRESS:${eureka.instance.hostname}:${server.port}}/eureka/
      
server:
  port: ${PORT:9001}