eureka:
  instance:
    hostname: ${HOST_NAME:localhost}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://${OTHER_ADDRESS_PORT}/eureka/
      
server:
  port: 9001