spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:session}
    username: ${DB_USERNAME:hoofkhanh}
    password: ${DB_PASSWORD:hoofkhanh}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
      
  data:  
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      database: ${REDIS_DB:1}
      
server:
  port: 9006
  
question-service:
  base-url: http://interview-system-question-service
  
user-service:
  base-url: http://interview-system-user-service
  
auth-service:
  base-url: http://interview-system-auth-service